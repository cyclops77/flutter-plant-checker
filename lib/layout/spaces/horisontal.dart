import 'package:flutter/material.dart';

class Horisontal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 20,
    );
  }
}

class HorisontalQuarter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 10,
    );
  }
}